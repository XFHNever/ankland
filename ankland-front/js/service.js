(function(window, $, undefined){
    var service = {
        attachEvents: function attachEvents(){

        },
        init: function(){
            this.attachEvents();
        }
    };

    service.init();

}(window || this, jQuery, undefined));
